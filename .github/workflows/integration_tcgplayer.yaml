name: Integration TCGPlayer

on:
  schedule:
    - cron: '16 7 * * 1' # https://crontab.guru/#16_7_*_*_1
  workflow_dispatch:

jobs:
  integration_tcgplayer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          python-version: 3.14.2
          poetry-version: 2.2.1
      - name: Run tests
        run: make test-integration WEBSITE=tcgplayer
